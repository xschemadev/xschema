# Ralph Progress Log
Started: Tue Jan 13 01:04:16 AM CET 2026
---

## Codebase Patterns

### Shared Cache Architecture (US-001)
- `fetcher.SharedCache` is the single source of truth for all fetched schemas
- Create cache in `generate.go`, pass to `retriever.Options.Cache` and `processor.Options.Cache`
- `metaschema.GetWithCache()` accepts cache parameter for deduplication
- Cache normalizes URIs internally (strips fragments)
- Thread-safe via `sync.RWMutex`

### Compiled Schema Cache (US-002)
- `validator.CompiledCache` caches compiled schemas by SHA256 content hash
- Hash key includes: schema content + draft hint + sorted metaschema URIs
- Pass cache via `ValidateOptions.Cache` field
- `CacheStats` tracks hits/misses/compiles for testing
- Thread-safe via `sync.RWMutex`

### Parallel Crawl Pattern (US-004)
- Wave-based: collect frontier → parallel fetch → extract refs → repeat
- Use `errgroup.WithContext()` + `g.SetLimit(concurrency)`
- Protect shared state (visited, newFrontier) with `sync.Mutex`
- Pass `Concurrency` via `processor.Options.Concurrency`
- Default: `min(runtime.NumCPU(), 8)`

---

## 2026-01-13 - US-001
- Implemented unified schema caching via `fetcher.SharedCache`
- Files changed:
  - cli/fetcher/fetcher.go - added SharedCache type with thread-safe Get/Set/Has/ToCache/Len
  - cli/fetcher/fetcher_test.go - new tests for SharedCache including race detection
  - cli/retriever/retriever.go - added Cache field to Options, uses SharedCache
  - cli/processor/processor.go - added Cache field to Options, crawlAndFetch/validateAll/bundleAll use SharedCache
  - cli/metaschema/metaschema.go - added GetWithCache() that checks shared cache before fetching
  - cli/cmd/generate.go - creates SharedCache and passes to both retriever and processor
  - cli/processor/processor_test.go - added TestSharedCache_DeclaredSchemaAlsoReferencedViaRef and TestSharedCache_CustomMetaschemaFetchedOnce
- **Learnings for future iterations:**
  - The old retriever had its own private `schemaCache` - removed in favor of injected SharedCache
  - processor.crawlAndFetch now returns error instead of (cache, error) since cache is passed in
  - metaschema still has internal parsed cache (map[string]*Metaschema) for vocabulary extraction - SharedCache is raw JSON only
  - validateAll/bundleAll use `cache.ToCache()` to get snapshot for iteration
---

## 2026-01-13 - US-005
- Verified RFC 6901 JSON pointer unescaping order
- Implementation was already correct: URI decode first, then JSON pointer unescape (~1→/, ~0→~)
- Files changed:
  - cli/bundler/bundler_test.go - added edge case tests for RFC 6901 compliance
- **Learnings for future iterations:**
  - RFC 6901 §6: when JSON pointer is in URI fragment, percent-decode first, then apply tilde unescaping
  - For key "literal~1key", encode as `~01` (escape the tilde)
  - For key "uri%2Fslash" (literal percent-2-F), encode as `%252Fslash` (double-encode the percent)
  - The PRD acceptance criteria was wrong - corrected to match actual RFC 6901 spec
---

## 2026-01-13 - US-007
- Verified $id scope handling was already correct
- Go's pass-by-value semantics ensure baseURI changes in nested processObject calls don't affect siblings
- Files changed:
  - cli/bundler/bundler_test.go - added TestBundleSiblingRefAfterID and TestBundleSiblingRefToParentPath
- **Learnings for future iterations:**
  - In processObject, when $id is found, baseURI is updated locally for that call only
  - Each property's children are processed with their own recursive call, so sibling branches don't share state
  - collectIDsAndAnchors has same pattern - baseURI mutations are local to each recursive call
  - PRD concern was unfounded - the implementation was already spec-compliant
---

## 2026-01-13 - US-006
- Fixed quadratic $defs flattening to O(n) linear time
- Root cause: old algorithm iterated over ALL rewrites for each def, causing O(n²) behavior
- New algorithm:
  - `collectDefsRecursive` returns newly added rewrite keys (not iterating all)
  - Single DFS pass collects all defs and builds complete rewrite map
  - Single pass rewrites all refs after collection
- Files changed:
  - cli/bundler/bundler.go - rewrote flattenDefs, collectDefsRecursive, added rewriteRefs, lookupRewrite, stripDefs
  - cli/bundler/bundler_test.go - added TestBundle5LevelNestedDefs, BenchmarkFlattenDefs, TestFlattenDefsLinearScaling
- **Learnings for future iterations:**
  - When building rewrite maps recursively, track newly added keys per call instead of iterating entire map
  - Linear scaling test: compare ratio of time(depth=10)/time(depth=5) - should be ~2 (linear), not ~4 (quadratic)
  - lookupRewrite uses longest prefix match for refs with sub-paths (e.g., #/$defs/X/properties/foo)
---

## 2026-01-13 - US-002
- Implemented compiled schema caching in validator
- Cache key: SHA256 hash of (schema content + draft hint + sorted metaschema URIs)
- Thread-safe via sync.RWMutex with separate read/write locks
- Files changed:
  - cli/validator/validator.go - added CompiledCache type, CacheStats, contentHash(), sortStrings()
  - cli/validator/validator.go - added Cache field to ValidateOptions
  - cli/validator/validator.go - ValidateSchemaWithOptions checks cache before compile, stores after
  - cli/validator/validator_test.go - added TestCompiledCache_SameSchemaTwice, TestCompiledCache_DifferentSchemas, TestCompiledCache_ConcurrentValidations, TestCompiledCache_WithExternalRef, TestCompiledCache_DraftHintAffectsCache
- **Learnings for future iterations:**
  - Cache key must include all factors that affect compilation: content, draft, metaschemas
  - Schemas with external refs still cache as valid (nil schema value) since the schema itself validated
  - Stats tracking (hits/misses/compiles) essential for testing cache behavior
  - sort.Strings would require import; simple O(n²) sort sufficient for small metaschema lists
---

## 2026-01-13 - US-003
- Implemented early validation of declared schemas before crawlAndFetch
- New processing flow:
  1. validateDeclared - validates schemas with standard metaschemas early (fail fast)
  2. crawlAndFetch - fetches external refs
  3. validateExternal - validates external schemas + declared schemas with custom metaschemas
  4. bundleAll - bundles schemas
- Files changed:
  - cli/processor/processor.go - added validateDeclared(), hasCustomMetaschema(), renamed validateAll→validateExternal
  - cli/processor/processor_test.go - added TestEarlyValidation_InvalidDeclaredSchemaFailsBeforeFetch, TestEarlyValidation_ValidSchemaWithExternalRefsProceedsToCrawl
- **Learnings for future iterations:**
  - Schemas with custom metaschemas must be skipped in early validation (metaschema not fetched yet)
  - hasCustomMetaschema() checks if $schema is non-standard using metaschema.IsStandardDraft()
  - validateExternal now validates both external schemas and declared schemas with custom metaschemas
---

## 2026-01-13 - US-004
- Implemented parallel crawl/fetch using wave-based errgroup pattern
- Wave flow: collect frontier URIs → parallel fetch all → extract refs → repeat
- Files changed:
  - cli/processor/processor.go - added Concurrency to Options, rewrote crawlAndFetch with errgroup.SetLimit
  - cli/processor/processor.go - added sync.Mutex for thread-safe visited/newFrontier maps
  - cli/processor/processor_test.go - made mockFetcher thread-safe with sync.Mutex
  - cli/processor/processor_test.go - added TestParallelCrawl_FiveExternalRefs, TestParallelCrawl_ConcurrentSafe, TestParallelCrawl_WaveBasedFetch
  - cli/cmd/generate.go - passes Concurrency to processor.Options
- **Learnings for future iterations:**
  - errgroup.SetLimit(n) controls max concurrent goroutines - same pattern as retriever
  - Default concurrency: min(runtime.NumCPU(), 8)
  - Wave-based approach: first wave may have N URIs, each fetched schema may discover more refs for next wave
  - Must protect shared maps (visited, newFrontier) with mutex when accessed from goroutines
  - mockFetcher needed sync.Mutex for fetchCalls slice to pass -race tests
---

## 2026-01-13 - US-008
- Fixed anchor path rewriting for deeply nested $defs (3+ levels)
- Root cause: rewriteAnchorPath only handled single-level flattening (/$defs/X → key__X)
- New algorithm:
  - Parse entire path to extract all nested def names (L1, L2, L3, ...)
  - Build fully flattened key: key__L1__L2__L3
  - Preserve any trailing path suffix (e.g., /properties/foo)
- Files changed:
  - cli/bundler/bundler.go - rewrote rewriteAnchorPath to iterate through all nested /$defs/ and /definitions/
  - cli/bundler/bundler_test.go - added TestBundleDeeplyNestedAnchorRef, TestBundleExternalSchemaWithDeeplyNestedInternalAnchorRef, getMapKeys helper
  - cli/bundler/testdata/schema-deep-anchor.json - test fixture with 3-level nested $defs and $anchor
- **Learnings for future iterations:**
  - Anchor paths are collected before flattening, so rewriteAnchorPath must compute the flattened key from the nested path
  - Pattern: extract all /$defs/Name segments, concatenate with __, preserve remaining path
  - Example: /$defs/key/$defs/L1/$defs/L2/$defs/Target → /$defs/key__L1__L2__Target
---

## 2026-01-13 - US-009
- Implemented vocabulary scope tracking in FilterSchema
- When nested $vocabulary is found in a subschema, it overrides parent scope for that subtree
- Implementation: filterObject checks for $vocabulary, updates validationEnabled/formatEnabled before filtering children
- Files changed:
  - cli/vocabulary/vocabulary.go - added parseVocab(), modified filterObject() to check for nested $vocabulary
  - cli/vocabulary/vocabulary_test.go - added 3 new tests: TestFilterSchema_NestedVocabOverride, TestFilterSchema_NestedVocabFormat, TestFilterSchema_DeepNestedVocab
- **Learnings for future iterations:**
  - Scope tracking is implicit via recursion - no explicit stack needed because Go's pass-by-value for bools means each recursive call gets its own copy
  - parseVocab() creates a fresh vocab map from $vocabulary field, then isVocabEnabled() determines what's enabled
  - Empty $vocabulary {} disables all vocabularies for that subtree
  - This pattern works because filterObject modifies local copies of validationEnabled/formatEnabled before recursing
---

## 2026-01-13 - US-010
- Implemented multiple output files per language support
- Added OutputFileSpec type with Path and Template fields
- Language.OutputFiles []OutputFileSpec is the preferred way (legacy OutputFile/Template still works via fallback)
- Files changed:
  - cli/language/language.go - added OutputFileSpec type, OutputFiles field to Language struct
  - cli/language/registry.go - added Unregister() method and package function for test cleanup
  - cli/injector/injector.go - Inject() now iterates over OutputFiles, executes each template, collects GeneratedFiles
  - cli/injector/injector.go - InjectClient() uses first OutputFile for import path (primary file)
  - cli/language/langs/typescript/typescript.go - migrated to use OutputFiles with single entry
  - cli/injector/injector_test.go - added testMultiFileLang(), TestInject_MultipleOutputFiles, TestInject_MultipleOutputFiles_StaleCleanup
- **Learnings for future iterations:**
  - Manifest-based stale cleanup already works for multi-file: each run writes all paths to manifest, next run compares
  - Fallback pattern: if len(OutputFiles) == 0, create synthetic slice from legacy OutputFile/Template
  - Test languages need SchemaURL to pass Register() validation
  - Unregister() useful for test cleanup to avoid registry pollution between tests
---
