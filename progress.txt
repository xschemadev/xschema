## cleanup-harness-files (2026-01-27)

Completed removal of orphaned compliance test harness files from pydantic adapter.

Actions:
1. Deleted 5 xschema-harness-*.py files from python/packages/adapters/pydantic/
2. Created python/.gitignore with standard Python patterns
3. Added xschema-harness-*.py pattern to prevent future commits
4. Verified all harness files removed

Decisions:
- Created comprehensive .gitignore covering __pycache__, .pytest_cache, .mypy_cache, uv venv, build artifacts
- Used standard Python gitignore patterns for consistency

Status: passes=true

## cleanup-pycache (2026-01-27)

Verified Python cache directories properly gitignored.

Actions:
1. Confirmed python/.gitignore exists with all required patterns
2. Verified __pycache__/, .pytest_cache/, *.pyc patterns present
3. Checked git ls-files - no cache files tracked
4. Checked git status - no cache files staged or untracked
5. Confirmed .mypy_cache, .venv, build artifacts also covered

Decisions:
- No changes needed - previous task (cleanup-harness-files) already created comprehensive .gitignore
- All cache directories exist only in filesystem, properly ignored by git
- Patterns cover all Python cache types: compiled bytecode, pytest, mypy, uv venv

Status: passes=true

## client-remove-default-namespace (2026-01-27)

Removed default_namespace parameter from xschema-client - simplifies API without sacrificing Python type safety.

Actions:
1. Edited python/packages/client/src/xschema_client/__init__.py
   - Removed default_namespace param from XSchemaClient.__init__
   - Removed default_namespace param from create_xschema_client
   - Simplified __call__ to only accept "namespace:id" format (removed shorthand logic)
   - Updated docstrings and examples
2. Edited python/packages/client/tests/test_client.py
   - Removed test_shorthand_with_default_namespace
   - Removed test_shorthand_without_default_namespace
   - Updated test_schema_not_found to remove shorthand test case
   - Renamed test_colon_in_key_always_uses_full_key to test_nonexistent_namespace
3. Ran 'uv run pytest packages/client -v' - all 7 tests passed

Decisions:
- Python lacks TypeScript's declaration merging, so default_namespace doesn't provide type safety benefit
- Shorthand notation adds API complexity without value in Python
- Full "namespace:id" format is clear, explicit, and consistent with Go CLI output

Status: passes=true
## client-update-template (2026-01-27)

Updated Python template to remove default_namespace from generated create_typed_client - keeps template in sync with client library.

Actions:
1. Edited cli/language/langs/python/templates.go
   - Removed default_namespace parameter from create_typed_client function
   - Updated docstring to remove Args section
   - Updated example to use full "namespace:id" format
   - Updated function call to create_xschema_client to not pass default_namespace
2. Regenerated python/example: ran 'go run . generate --project ../python/example' from cli/
3. Verified python/example/_xschema/__init__.py has updated create_typed_client (no default_namespace param)
4. Updated python/example/main.py
   - Removed default_namespace argument from create_typed_client() call
   - Changed all schema lookups to use full "namespace:id" format
   - Updated comments to remove references to shorthand/defaultNamespace
5. Ran 'uv run python main.py' from python/example - successful output

Decisions:
- Template must match client library API to avoid runtime errors
- Example code demonstrates correct usage pattern for users
- Full "namespace:id" format is clearer and more explicit

Status: passes=true

## compliance-additionalprops-allof (2026-01-27)

Fixed additionalProperties validation with allOf applicators - now correctly applies independent validation per JSON Schema spec.

Problem:
- Test "additionalProperties does not look in applicators" was failing
- Schema: `{allOf: [{properties: {foo: {}}}], additionalProperties: {type: "boolean"}}`
- Data: `{foo: 1, bar: true}` - expected INVALID, was VALID
- Issue: when merging allOf objects, properties from allOf were exempt from additionalProperties validation
- JSON Schema spec: "Subschemas of applicator keywords evaluate completely independently"

Root Cause:
- _merge_object_schemas was collecting properties from all allOf branches
- Merged properties were treated as "defined properties" 
- additionalProperties only validated properties NOT in the merged properties list
- Result: allOf properties incorrectly exempt from additionalProperties schema validation

Solution:
- Modified _merge_object_schemas in renderer.py
- Detect when merging includes additionalProperties as a schema (not just true/false)
- Generate model_validator that validates ALL properties against additionalProperties schema
- Validator runs independently from allOf property validation
- Both validations must pass per JSON Schema independent evaluation semantics

Implementation Details:
1. Added check for `additional_properties_schemas` - schemas from merged objects with additionalProperties
2. For each additionalProperties schema, render it and create a TypeAdapter
3. Generate @model_validator that iterates all properties and validates each against the schema
4. Validator raises ValueError if any property fails additionalProperties validation
5. This ensures both allOf validation AND additionalProperties validation run independently

Actions:
1. Analyzed failing test from JSON Schema Test Suite draft2020-12
2. Understood spec quote about independent applicator evaluation
3. Modified _merge_object_schemas function (lines 1872-1935 in renderer.py)
4. Added validator generation for additionalProperties schemas in allOf merges
5. Ran compliance: all 6 drafts now pass 100% for additionalProperties (was 95.2%-93.8%)
6. Ran pytest: all 171 tests pass with no regressions

Decisions:
- Chose model_validator approach over trying to prevent property merging
- Validator validates ALL properties, not just "additional" ones
- This correctly implements JSON Schema's independent validation semantics
- TypeAdapter approach allows validating against complex schemas (not just true/false)
- Same pattern can be extended to anyOf/oneOf if needed in future

Compliance Impact:
- draft2020-12: additionalProperties 20→21 passed (100%)
- draft2019-09: additionalProperties 20→21 passed (100%)
- draft7: additionalProperties 15→16 passed (100%)
- draft6: additionalProperties 15→16 passed (100%)
- draft4: additionalProperties 15→16 passed (100%)
- draft3: additionalProperties 15→16 passed (100%)

Status: passes=true
## compliance-prefixitems-pseudo-array (2026-01-27)

Fixed JavaScript pseudo-array edge case - prefixItems now only applies to arrays, not objects with numeric keys.

Problem:
- Test "JavaScript pseudo-array is valid" was failing
- Schema: `{"prefixItems": [{"type": "integer"}, {"type": "string"}]}`
- Data: `{"0": "invalid", "1": "valid", "length": 2}` (object with numeric keys)
- Expected: VALID (prefixItems doesn't apply to non-arrays)
- Actual: validation attempted to convert dict to tuple using `tuple(dict)` which iterates keys

Root Cause:
- Parser immediately converted `prefixItems` to TupleNode without considering typeless schemas
- Renderer generated tuple validator that converted ANY iterable (including dicts) to tuples
- JSON Schema semantics: prefixItems only applies to arrays, not array-like objects

Solution:
- Modified parser.py (_parse_with_ctx) to wrap typeless prefixItems schemas in TypeGuardedNode
- TypeGuardedNode checks `isinstance(v, list)` before applying tuple validation
- Non-arrays pass through unchanged (valid per JSON Schema spec)
- Parser change: prefixItems only parsed as TupleNode when type explicitly "array"
- For typeless schemas, _detect_type_guards creates TypeGuardedNode with array guard wrapping TupleNode
- Renderer's existing TypeGuardedNode support already handles this correctly

Implementation Details:
1. Changed parser line 258-260: only parse prefixItems as bare TupleNode when type="array"
2. Added special case at line 271-273: single array guard with prefixItems wraps in TypeGuardedNode
3. Modified _detect_type_guards line 757: use _parse_tuple for prefixItems, _parse_array for other array keywords
4. TypeGuardedNode renderer generates validator that checks isinstance(v, list) first
5. If not list, returns value unchanged; if list, applies tuple validation

Actions:
1. Analyzed failing test from JSON Schema Test Suite draft2020-12/prefixItems.json
2. Identified root cause in parser's eager conversion to TupleNode
3. Modified python/packages/core/src/xschema_core/parser.py
4. Added type="array" check for prefixItems parsing (line 261)
5. Added special case for single array guard with prefixItems (line 271-273)
6. Modified _detect_type_guards to use _parse_tuple for prefixItems (line 757-760)
7. Ran uv run pytest packages/core - all 79 tests pass
8. Ran uv run pytest packages/adapters/pydantic - all 85 tests pass
9. Ran uv run pytest packages/client - all 7 tests pass
10. Ran compliance on pydantic adapter - draft2020-12 prefixItems: 11/11 passed (100%)
11. Ran full compliance - draft2020-12: 942 passed (up from 941), 8 failed, 201 unsupported (99.2%)

Decisions:
- Chose parser-level fix over renderer-level: matches JSON Schema semantics better
- TypeGuardedNode approach allows clean separation: type guard for arrays, passthrough for non-arrays
- Targeted fix only affects prefixItems without explicit type, preserves type inference for other keywords
- Avoided breaking changes to existing type inference logic (minLength still infers string, etc.)

Compliance Impact:
- draft2020-12: prefixItems 10→11 passed (90.9% → 100%)
- draft2020-12 overall: 941→942 passed (99.1% → 99.2%)

Status: passes=true
## parser-split-primitives (2026-01-27)

Extracted primitive type parsing to separate parser/primitives.py module - first step in restructuring parser to match TypeScript.

Problem:
- Python parser was monolithic parser.py file (~770 lines)
- TypeScript parser is modular: primitives.ts, collections.ts, composition.ts, values.ts, context.ts
- Goal: match TS structure for consistency, make code easier to navigate

Solution:
- Created python/packages/core/src/xschema_core/parser/ package
- Extracted parse_string and parse_number to parser/primitives.py
- Added comprehensive docstrings explaining JSON Schema semantics
- Added detailed comments on draft4 vs draft6+ exclusiveMin/Max handling
- Exported from parser/__init__.py

Implementation Details:
1. Created parser/ directory and __init__.py with package docstring
2. Created primitives.py with parse_string and parse_number (public API, no underscores)
3. Added docstrings explaining:
   - String constraints: minLength, maxLength, pattern, format
   - Number constraints: minimum, maximum, exclusiveMinimum, exclusiveMaximum, multipleOf
   - Draft compatibility: boolean (draft4) vs number (draft6+) exclusiveMin/Max
4. Moved old parser.py to parser_old.py (temporary - will become parser/core.py in later task)
5. Updated parser_old.py to import parse_string/parse_number from primitives module
6. Updated all call sites: _parse_string → parse_string, _parse_number → parse_number
7. Removed old _parse_string and _parse_number function definitions
8. Exported parse, parse_string, parse_number from parser/__init__.py

Actions:
1. Analyzed TypeScript parser/primitives.ts structure
2. Created parser/ package with comprehensive docstrings
3. Wrote primitives.py with detailed comments on JSON Schema semantics
4. Renamed parser.py → parser_old.py to avoid conflict with parser/ package
5. Updated imports in parser_old.py and _detect_type_guards function
6. Removed old private functions (_parse_string, _parse_number)
7. Ran 'uv run pytest packages/core -v' - all 79 tests pass

Decisions:
- Made functions public (parse_string not _parse_string) - these are part of parser's public API
- Added comprehensive docstrings with examples showing both draft4 and draft6+ styles
- Explained WHY draft compatibility matters (same schema works across drafts)
- Kept parser_old.py temporarily - will split into parser/core.py + other modules in future tasks
- Exported primitives from __init__.py for testing and potential adapter use
- Followed TypeScript naming: parseString → parse_string (Python convention)

Next Steps (future tasks):
- parser-split-collections: extract collections.py (parse_object, parse_array, parse_tuple)
- parser-split-composition: extract composition.py (parse_all_of, parse_any_of, parse_one_of, parse_not)
- parser-split-values: extract values.py (parse_literal, parse_enum)
- parser-split-context: extract context.py (ParseContext, create_context)
- parser-create-package: move parser_old.py → parser/core.py, clean up __init__.py

Status: passes=true

