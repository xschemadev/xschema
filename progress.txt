# Ralph Progress Log
Started: Mon Jan 12 02:12:14 AM CET 2026

## Codebase Patterns

- **Bundler architecture**: processNode → processObject → processRef for external refs. After fetching a remote schema, flattenDefs() extracts nested $defs/definitions and adds them to bctx.defs with prefixed keys.
- **Def flattening**: Nested definitions are flattened using `__` separator (e.g., `remote__Address`). Both internal refs and external fragment refs are rewritten to the flattened location.
- **Ref rewriting flow**: flattenDefs → rewriteNestedRefs (for flattened defs) + parseDefFragment (for external fragments pointing to defs)

---
## 2026-01-12 - US-001
- Implemented metaschema bundling skip
- Files changed: cli/bundler/bundler.go
- **Learnings for future iterations:**
  - Metaschema URLs follow pattern: http(s)://json-schema.org/draft-*/... or https://json-schema.org/draft/*/...
  - Metaschemas use $recursiveAnchor which is in forbiddenKeywords - this is why they fail
  - Simple approach: detect early in processRef() and return obj unchanged
---

## 2026-01-12 - US-002 & US-004
- Implemented nested definition flattening and fragment ref rewriting
- Files changed: cli/bundler/bundler.go, cli/bundler/bundler_test.go
- **What was implemented:**
  - `flattenDefs()` - extracts $defs/definitions from embedded schemas, prefixes keys with parent key using `__`
  - `rewriteNestedRefs()` - rewrites refs in flattened defs to point to new locations
  - `parseDefFragment()` - detects `/definitions/X` or `/$defs/X` fragments and extracts the def name
  - Fragment refs like `foo.json#/definitions/Bar` now rewritten to `#/$defs/foo_key__Bar`
- **Learnings:**
  - US-002 and US-004 are tightly coupled - can't flatten defs without also fixing fragment refs
  - Tests needed updating to expect flattened structure instead of nested paths
  - Recursive flattening handles defs with their own nested defs
---

## 2026-01-12 - US-005
- Implemented $vocabulary support for disabling validation
- Files changed: typescript/packages/core/src/schema/json-schema.ts, typescript/packages/core/src/parser/index.ts
- **What was implemented:**
  - Added `$vocabulary?: Record<string, boolean>` to JSONSchema interface
  - Added `isValidationDisabled()` helper that checks validation vocab URIs
  - In `parseSchema()`, returns `{ kind: 'any' }` when validation is disabled
- **Learnings:**
  - Validation vocab URIs: draft-2019-09/2020-12 use different vocab URIs
  - Check early in parseSchema before any keyword processing
---

## 2026-01-12 - US-006
- Cleaned up adapter READMEs to remove verbose limitations docs
- Files changed:
  - typescript/packages/adapters/zod/README.md
  - typescript/packages/adapters/effect/README.md
  - typescript/packages/adapters/arktype/README.md
  - typescript/packages/adapters/valibot/README.md
- **What was done:**
  - Removed entire Known Limitations section from zod, effect, arktype READMEs
  - Kept only JavaScript Prototype Property Names subsection in valibot README (Valibot-specific bug users need to know)
  - Preserved all other content (installation, usage examples, etc.)
- **Learnings:**
  - Compliance reports are the single source of truth for limitations
  - READMEs should be concise, point to compliance reports for details
---

## 2026-01-12 - US-007
- Ran compliance verification for all adapters
- **Critical fix:** Rebuilt CLI to include updated unsupported-features.json (was out of date due to go:embed)
- Files changed: cli/xschema (rebuilt binary)
- **Results after filtering unsupported features:**
  - Zod: 97.6% (870/891 for draft2020-12, 858/879 for draft2019-09)
  - Effect: 97.6% (identical to Zod)
  - ArkType: 97.6% (identical to Zod)
  - Valibot: 96.5% (10 extra failures per draft due to JS prototype property names)
- **Remaining failures (bundler bugs not addressed by previous PRD items):**
  1. refs with relative URIs and nested $defs in embedded schemas
  2. URN refs with nested pointer refs
  3. Anchor resolution in chained remote refs
  4. One $vocabulary edge case test still failing
- **Learnings:**
  - go:embed compiles file at build time - must rebuild CLI when updating embedded files
  - unsupported-features.json properly excludes dynamic/recursive refs (73 tests total)
  - Remaining failures are in bundler, shared by all adapters - not adapter-specific
  - PRD acceptance criteria NOT MET - need additional bundler fixes
---
