# Ralph Progress Log
Started: Mon Feb  9 10:59:29 PM CET 2026
---

## Task: Add XSchemaAdapter type to @xschemadev/core
- Added `XSchemaAdapter` interface to `typescript/packages/core/src/types.ts` with `readonly __xschema: true`, `readonly id: string`, `readonly name: string`
- Re-exported from `index.ts` as type export
- Build + typecheck pass, type is importable from `@xschemadev/core`

## Task: Add adapter export to all 4 adapter packages
- Added `export const adapter = { __xschema: true, id: '...', name: '...' } as const satisfies XSchemaAdapter` to zod, arktype, effect, valibot
- Pattern: import `XSchemaAdapter` type from `@xschemadev/core`, export const at module level before `convert` function
- Build + typecheck pass

## Task: Add Go CLI convert subcommand
- Created `cli/cmd/convert.go` with cobra command registered on rootCmd
- Flags: --adapter (required), --lang (default "typescript"), --project (default cwd), --verbose, --allow-fetch
- Reads stdin as `[]ConvertSchemaInput`, converts to `[]retriever.RetrievedSchema` with inline source
- Runs processor.Process() then generator.GenerateAll(), outputs JSON to stdout
- Without --allow-fetch, external $refs return error suggesting the flag
- JSON error objects written to stderr on failure
- Empty array input returns empty array output
- go build, go vet, go test all pass
- End-to-end verified: piped JSON schema through zod adapter, got valid ConvertResult output

## Codebase Patterns

### Testing convert command with cobra
- Refactored convert.go to use `cmd.InOrStdin()` / `cmd.OutOrStdout()` / `cmd.ErrOrStderr()` instead of `os.Stdin`/`os.Stdout`/`os.Stderr` so tests can inject I/O via `rootCmd.SetIn/SetOut/SetErr`
- For integration tests needing the adapter binary: create temp dir with empty `package.json`, `bun.lock`, and symlink adapter CLI into `node_modules/.bin/xschema-<name>`
- Use `runtime.Caller(0)` to find adapter CLI path relative to test file

## Task: Add Go CLI convert subcommand tests
- Created `cli/cmd/convert_test.go` with 6 tests covering all PRD requirements
- Refactored `convert.go`: writeJSONError now takes `*cobra.Command`, all I/O uses cobra's InOrStdin/OutOrStdout/ErrOrStderr
- Integration tests (valid single schema, multiple schemas) use symlinked adapter CLI in temp project dir, skip with `testing.Short()`
- Unit tests: missing --adapter flag, invalid JSON stdin, empty array, external $ref without --allow-fetch
- All tests pass, go vet clean

## Task: Create @xschemadev/runtime package scaffold
- Created `typescript/packages/runtime/` with package.json, tsconfig.json, src/index.ts
- Follows existing patterns: extends tsconfig.base.json, workspace:* dep on core, type module
- Re-exports ConvertResult and XSchemaAdapter types from @xschemadev/core
- Workspace auto-discovered via `packages/*` glob in root package.json
- Build, typecheck, workspace resolution all verified

## Task: Implement @xschemadev/runtime convert function
- Implemented full convert() function in `typescript/packages/runtime/src/index.ts`
- Exports: SchemaInput, ConvertOptions, ConvertError, convert, plus re-exports of ConvertResult and XSchemaAdapter
- convert() spawns xschema CLI as child process, pipes JSON via stdin/stdout
- ConvertError extends Error with `stderr` property for debugging
- Supports single schema or array input (normalized internally)
- Options: adapter (required XSchemaAdapter), cwd, executablePath, allowFetch
- Build + typecheck pass, all types verified in generated .d.ts

## Task: Write runtime docs - overview page
- Replaced placeholder "coming soon" content in web/content/docs/runtime/index.mdx
- Added: quick example with real API (convert + adapter import), "When to Use" table, "Framework vs Runtime" comparison, Prerequisites section, Cards linking to getting-started and api-reference
- Imports match existing docs patterns (fumadocs-ui Callout/Card/Cards, lucide-react icons)
- types:check passes (only pre-existing radix-ui error in separator.tsx)

