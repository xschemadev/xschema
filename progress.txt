# Ralph Progress Log
Started: Sun Jan 11 03:05:49 PM CET 2026
---

## Codebase Patterns
- core parser hard-fails are discoverable via `throw new Error` / `is not supported` searches in `typescript/packages/core/src/parser/` and `typescript/packages/core/src/utils/json-pointer.ts`
- keyword behavior is split between `normalizeDeep` (legacy rewrites) and the parser; check both before assuming a keyword is "ignored"
- workspace packages resolve `@xschemadev/core` via built `dist`, so run `bun run build` after core api/type changes
---

## 2026-01-11 - US-001
- Verified bootstrap discovery workflow was already implemented
- folder `tasks/adapters-optimization/` exists
- README.md has required sections (discovery doc locations, required sections, the rule for adding stories)
- typecheck passes
- Files changed: prd.jsonc (marked passes: true)
- **Learnings for future iterations:**
  - US-001 was done in prior commit c13f8b4, just needed verification
  - discovery workflow README defines clear categories: core-missing, adapter-native, forced-emulation, not-supported
---

## 2026-01-11 15:31:08 CET - US-002
- Implemented `tasks/adapters-optimization/core-discovery.md` (baseline behavior, throw list, preservation proposal, migration notes)
- Inserted 2 follow-up implementation stories derived from discovery, renumbered subsequent story ids
- Files changed: `tasks/adapters-optimization/core-discovery.md`, `prd.jsonc`, `progress.txt`
- **Learnings for future iterations:**
  - current core non-total points: `unevaluatedItems`, `unevaluatedProperties`+applicators, external `$ref`, missing pointer targets
  - `unevaluatedProperties` is partially handled in normalizer (rewritten to `additionalProperties` only in simple cases)
  - adapters call `parse()` directly today, so avoid breaking that api; prefer additive `parseWithIssues`
---

## 2026-01-11 15:41:37 CET - US-003
- Implemented total core parsing: `parse()` now returns `{ node, issues }` and never throws
- Captured non-fatal issues for `unevaluatedItems`, `unevaluatedProperties`+applicators, and `$ref` resolution failures
- Updated all TS adapters to consume `parse().node`
- Files changed: `typescript/packages/core/src/parser/context.ts`, `typescript/packages/core/src/parser/index.ts`, `typescript/packages/core/src/index.ts`, `typescript/packages/adapters/*/src/index.ts`, `prd.jsonc`, `progress.txt`
- **Learnings for future iterations:**
  - `ParseIssue` currently carries `code`, `message`, plus optional `keyword`/`ref` (no schema path yet)
  - `$ref` failures are cached as `any` to avoid repeated throws
  - workspace packages resolve `@xschemadev/core` from built `dist`
---

## 2026-01-11 - US-004
- Reverted core parser to throw on unsupported keywords (after US-003 made it total)
- Added throws for: unevaluatedItems, unevaluatedProperties+applicators, $dynamicRef, $dynamicAnchor, $recursiveRef, $recursiveAnchor
- Simplified ParseIssue to only `ref_resolution_failed` (no more `unsupported_keyword`)
- Files changed: `typescript/packages/core/src/parser/context.ts`, `typescript/packages/core/src/parser/index.ts`, `prd.jsonc`, `progress.txt`
- **Learnings for future iterations:**
  - US-003 made parsing total, US-004 intentionally re-adds throws for keywords with evaluation semantics
  - ParseResult still exists: `{ node, issues }` where issues are only ref resolution failures now
  - bundler (US-011) will enforce ref validation pre-adapter
---

## 2026-01-11 - US-011
- cli bundler now validates schemas before passing to adapters
- rejects $dynamicRef, $dynamicAnchor, $recursiveRef, $recursiveAnchor at bundle time
- validates all internal refs (#...) resolve to valid json pointer targets
- errors on external $ref without base uri (relative file refs can't resolve against cwd)
- Files changed: `cli/bundler/bundler.go`, `cli/bundler/bundler_test.go`, `prd.jsonc`, `progress.txt`
- **Learnings for future iterations:**
  - json pointer validation happens after bundling (embedded refs get rewritten first)
  - forbiddenKeywords check happens in processObject before any recursion
  - base uri check happens in processRef for non-absolute refs
---

## 2026-01-11 - US-012
- reverted parse() to return SchemaNode directly (removed ParseResult wrapper)
- removed ParseIssue type entirely (bundler handles ref validation now)
- simplified parseRef to let resolution errors throw (bundler catches these)
- updated all 6 adapters to use `const ir = parse(schema as JSONSchema)`
- Files changed: `typescript/packages/core/src/parser/context.ts`, `typescript/packages/core/src/parser/index.ts`, `typescript/packages/core/src/index.ts`, `typescript/packages/adapters/*/src/index.ts`, `prd.jsonc`, `progress.txt`
- **Learnings for future iterations:**
  - api simplification was possible because us-011 moved ref validation to bundler
  - ParseContext still exists for internal state (refs cache, processing set, version)
  - now that bundler validates refs, parser can throw on resolution failure
---

## 2026-01-11 - US-005
- ran zod compliance tests: draft2020-12 92.8%, draft2019-09 92.1%, draft7 97.2%, draft6 96.9%, draft4 97.2%, draft3 95.9%
- wrote `tasks/adapters-optimization/zod-discovery.md` with full failing test list and categorized failures
- added 3 bundler improvement stories (US-006, US-007, US-008) and renumbered subsequent stories
- Files changed: `tasks/adapters-optimization/zod-discovery.md`, `prd.jsonc`, `progress.txt`
- **Learnings for future iterations:**
  - zod adapter is solid; most failures (~100 of 220) are bundler ref-handling issues
  - draft3/4 boolean exclusiveMax/Min failures are version-detection bugs: compliance tests lack `$schema`, parser defaults to draft-2020-12
  - bundler needs: $schema injection, URI-decoded pointer segments, $anchor fragment resolution
  - dynamic/recursive refs intentionally unsupported (us-004, us-011 decisions)
---

## 2026-01-11 - US-007
- bundler now URI-decodes JSON pointer segments per RFC 6901 §6
- added url.PathUnescape before JSON pointer escape processing (~1,~0)
- added tests for percent/slash/tilde/quote/space encoded refs
- Files changed: `cli/bundler/bundler.go`, `cli/bundler/bundler_test.go`, `prd.jsonc`, `progress.txt`
- **Results:**
  - draft2020-12: 92.8% → 93.6% (+8 tests)
  - draft2019-09: 92.1% → 92.8% (+8 tests)
  - draft7: 97.2% → 98.1% (+8 tests)
  - draft6: 96.9% → 97.9% (+8 tests)
  - draft4: 97.2% → 98.6% (+8 tests)
  - draft3: 95.9% → 97.3% (+6 tests)
  - total: 46 tests fixed (more than estimated 30)
- **Learnings for future iterations:**
  - URI encoding applies to fragment (after #), JSON pointer escapes (~0,~1) apply after URI decoding
  - refs with quotes (%22) also fixed by same mechanism
  - remaining ref failures are: $anchor resolution (US-008), relative URI with $id scoping, URN refs
---

## 2026-01-11 - US-006
- bundler now injects $schema URI based on draft when schema lacks one
- added Draft field to bundler.Options
- compliance runner passes draft name to bundleSchema
- Files changed: `cli/bundler/bundler.go`, `cli/bundler/bundler_test.go`, `cli/compliance/runner.go`, zod compliance results, `prd.jsonc`
- **Results:**
  - draft4: 98.6% → 98.9% (+2 tests)
  - draft3: 97.3% → 97.8% (+2 tests)
- **Learnings for future iterations:**
  - $schema injection only applies to root schema objects, not boolean schemas
  - draftToSchemaURI map covers draft3 through draft2020-12
  - ts core parser uses $schema to determine exclusiveMin/Max boolean vs number behavior
---

## 2026-01-11 - US-008
- bundler now resolves $anchor fragment refs (location-independent identifiers)
- collects $anchor declarations and fragment-only $id/#foo during first pass
- rewrites anchor refs like #foo to their JSON pointer paths before validation
- handles both $anchor (draft2019-09+) and fragment $id (draft4/6/7)
- Files changed: `cli/bundler/bundler.go`, `cli/bundler/bundler_test.go`, `prd.jsonc`, `progress.txt`
- **Results:**
  - draft7: 98.1% → 98.8% (+4 tests)
  - draft6: 97.9% → 98.7% (+4 tests)
  - draft4: 98.9% → 99.6% (+2 tests)
  - total: 10 tests fixed
- **Learnings for future iterations:**
  - in draft4/6/7, `$id: "#foo"` works same as `$anchor: "foo"` in draft2019-09+
  - anchors map stores anchor name → JSON pointer path
  - remaining refRemote failures are $id scoping issues (separate bundler limitation)
---

## 2026-01-11 - US-009
- ran valibot compliance tests: draft2020-12 85.0%, draft2019-09 84.0%, draft7 88.7%, draft6 87.6%, draft4 87.0%, draft3 89.7%
- wrote `tasks/adapters-optimization/valibot-discovery.md` with full failing test list and categorized failures
- added 3 valibot adapter improvement stories (US-010, US-011, US-012) and renumbered subsequent stories
- Files changed: `tasks/adapters-optimization/valibot-discovery.md`, `prd.jsonc`, compliance results, `progress.txt`
- **Results:**
  - valibot baseline is 85-90% vs zod's 93-99%
  - ~100 tests fail due to type coercion (validators reject wrong types instead of ignoring)
  - ~5 tests fail due to propertyNames not being enforced
  - 6 tests fail due to type:object accepting arrays
  - ~14 tests fail due to JS prototype property names (valibot bug)
- **Learnings for future iterations:**
  - valibot validators reject wrong types instead of ignoring them (JSON Schema says keywords only apply to matching types)
  - `v.object()` accepts arrays but JSON Schema "object" excludes arrays
  - properties named `__proto__`, `constructor`, `toString` cause valibot runtime errors
  - tuple handling (items/prefixItems as arrays) has multiple issues
  - dynamic/recursive ref failures (~67 tests) same as zod - bundler limitation
---

## 2026-01-11 - US-010
- fixed valibot type-coercion passthrough: renderTypeGuarded now guards validators by type check
- JSON Schema says keywords only apply to matching types; non-matching types should pass
- before: renderTypeGuarded just rendered union of schemas, ignoring type guards
- after: uses v.pipe(v.any(), v.check(...)) with typeof checks per guard
- Files changed: `typescript/packages/adapters/valibot/src/renderer.ts`, compliance results, `prd.jsonc`, `progress.txt`
- **Results:**
  - draft2020-12: 85.0% → 90.0% (+46 tests)
  - draft2019-09: 84.0% → 89.2% (+47 tests)
  - draft7: 88.7% → 94.1% (+45 tests)
  - draft6: 87.6% → 93.5% (+45 tests)
  - draft4: 87.0% → 94.0% (+40 tests)
  - draft3: 89.7% → 95.6% (+24 tests)
  - total: ~247 tests fixed
- **Learnings for future iterations:**
  - TypeGuardedNode is generated by core parser when type-specific keywords exist without explicit type
  - zod adapter correctly renders type guards; valibot was missing this logic
  - remaining failures: bundler limitations, JS prototype props bug, tuple handling, propertyNames, type:object accepts arrays
---
