---
title: CLI Reference
description: xschema CLI commands and flags
---

import { Callout } from "fumadocs-ui/components/callout";

## generate

Parse config files, convert schemas, and output native validators.

```bash
xschema generate [flags]
```

### Flags

| Flag | Short | Default | Description |
|------|-------|---------|-------------|
| `--project` | `-p` | `.` | Project root directory |
| `--output` | `-o` | `.xschema` | Output directory |
| `--lang` | | auto | Filter to specific language |
| `--verbose` | `-v` | `false` | Show detailed output |
| `--dry-run` | | `false` | Preview without writing files |
| `--concurrency` | `-c` | `8` | Parallel schema fetches |
| `--env-file` | | `.env` | Path to env file for header variables |

### Examples

```bash
# Generate in current directory
xschema generate

# Specify project root
xschema generate --project ./my-app

# Custom output directory
xschema generate --output ./generated

# Debug output
xschema generate --verbose

# Preview without writing
xschema generate --dry-run
```

<Callout type="info">
Watch mode (`--watch`) is planned.
</Callout>
