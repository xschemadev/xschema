{
  "name": "xschema-typescript",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --filter '@xschemadev/core' build && bun run --filter '@xschemadev/client' build && bun run --filter '@xschemadev/zod' build",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
