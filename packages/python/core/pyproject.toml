[project]
name = "xschema"
version = "0.0.1"
description = "XSchema runtime for Python"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/xschema"]
