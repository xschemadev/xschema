[project]
name = "xschema-client"
version = "0.1.0"
description = "Runtime client for xschema-generated validators with type-safe schema lookup"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
